export const environment = {
    production: true,
    serverBase: "http://localhost:3001",
    inSecureApi: "/api",
    secureApi: "/auth",
    endpoints: {
        users: "/getAllUsers",
        admin: "/admin",
        pager: "/pager"
    }
};
